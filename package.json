{
  "name": "albums-to-listen-to",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A small public-ready album listening list app with accounts, sharing, collaboration, and ratings.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/config.js && node --check src/explore-data.js && node --check public/app.js",
    "audit": "npm audit --omit=dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
